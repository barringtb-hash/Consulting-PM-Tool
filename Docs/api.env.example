# API service environment variables (place in pmo/apps/api/.env during local dev)

# Database configuration
# Prisma's schema defaults to PostgreSQL; the test harness can rewrite it for SQLite when needed.
# Use DATABASE_PROVIDER to signal which database the API should expect (e.g., search behaviour)
# and to let the Vitest setup choose between Postgres and SQLite. For normal development and
# production, keep this set to "postgresql"; use "sqlite" only for the test fallback.
DATABASE_PROVIDER="postgresql"

# PostgreSQL connection string (default)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/postgres"

# SQLite database path used by Prisma (relative to the monorepo root) when DATABASE_PROVIDER=sqlite
# DATABASE_URL="file:./dev.db"

# Auth and security
JWT_SECRET="replace-with-a-long-random-secret"
JWT_EXPIRES_IN="1h"
BCRYPT_SALT_ROUNDS=10

# Server configuration
PORT=4000
NODE_ENV="development"
