# Base URL for API requests.
#
# DEVELOPMENT:
# Leave this commented out to use the Vite proxy (recommended for local development).
# The proxy automatically forwards /api requests to http://localhost:4000.
#
# PRODUCTION (Vercel):
# Set this to your Render API's public base URL, e.g.:
# VITE_API_BASE_URL="https://your-api.onrender.com/api"
#
# ALTERNATIVE DEVELOPMENT (without proxy):
# If you need to test cross-origin requests locally, uncomment and use:
# VITE_API_BASE_URL="http://localhost:4000/api"

# ============================================================================
# MODULE CONFIGURATION
# ============================================================================
# Comma-separated list of enabled modules (defaults to all if not set)
# Core modules (always enabled): dashboard, tasks, clients, projects
# Toggleable modules: assets, marketing, leads, pipeline, admin
#
# Examples:
#   All modules:    VITE_ENABLED_MODULES=dashboard,tasks,clients,projects,assets,marketing,leads,pipeline,admin
#   Basic CRM:      VITE_ENABLED_MODULES=dashboard,tasks,clients,projects
#   Sales focused:  VITE_ENABLED_MODULES=dashboard,tasks,clients,projects,leads,pipeline
#
# Note: Module dependencies are auto-enabled (e.g., marketing requires clients,projects)
# Note: Per-tenant overrides from API take precedence over this default
VITE_ENABLED_MODULES=
